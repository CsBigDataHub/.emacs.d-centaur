(use-package eyebrowse
  :demand
  :config
  (eyebrowse-mode)
  )
;; (use-package winds
;;   :demand
;;   :custom
;;   (winds-default-ws 1)
;;   (winds-default-cfg 1)
;;   )
(provide 'init-eyebrowse)
